---
plan: plan-reveal-self-guided-rework-2026-07-21-7cd8.md
generated: 2026-07-21
status: in-progress
---

# Tasks

## Phase 1: Shared read-state foundation

- [x] T001 Add two reveal-only fields to `Room` in `shared/src/types.ts`: `bookReads: Record<string, string[]>` (FK Book.id -> deduped Player.id[] who completed a read — opened then closed that book's modal; empty `{}` outside reveal) and `currentlyReading: Record<string, string>` (FK Player.id -> Book.id currently open in that player's modal; absent key = no modal open; cleared on disconnect). Document both with the keyed-by rationale (bookReads keyed by Book.id because both consumers aggregate per book; currentlyReading keyed by Player.id — one open book per reader). Initialize both to `{}` in `createRoom` and `replayRoom` in `server/src/domain/roomStore.ts`. Test: a freshly created and a replayed room both start with empty `bookReads`/`currentlyReading`. [artifacts: datamodel]
- [x] T002 Add `onSetReadingBook` to `server/src/socket/handlers.ts` and wire it in `server/src/socket/server.ts`. Input `{ roomId, playerId, bookId: string | null }`, last-write-wins semantics: non-null `bookId` sets `currentlyReading[playerId] = bookId` (modal opened); `null` (or switching) appends `playerId` deduped to `bookReads[prevBookId]` (the player's prior `currentlyReading` value, if any) and deletes `currentlyReading[playerId]`. Guards: `room-not-found`, reject when `room.status !== 'reveal'`, reject an unknown `bookId`. Follow the handler convention: guard -> mutate -> `logger.log({ event: 'book_read_state', outcome: 'success', roomId, playerId, bookId })` -> `socket.to(roomId).emit('roomUpdated', { room })` -> `ack({ room })`. Tests (test-first, Principle III): open sets `currentlyReading`; close appends deduped to `bookReads` and clears `currentlyReading`; a second close for the same already-recorded player does not duplicate; guards reject non-reveal status and unknown book. [artifacts: infrastructure]
- [x] T003 Extend `onDisconnect` in `server/src/socket/handlers.ts` to `delete room.currentlyReading[playerId]` for the departing player (so the "being read by" badge does not leak), WITHOUT crediting a completed read (a disconnect is not a chosen close — the player will reopen on reconnect). Preserve the existing `Player.connected` flip. Test: a player disconnected mid-read is removed from `currentlyReading` and is NOT added to that book's `bookReads`. [artifacts: infrastructure]
- [x] T004 Revise `datamodel.md`: add the `bookReads` and `currentlyReading` field rows to the `Room` entity (reveal-only, keyed-by rationale, cleared/reset behavior), and add a Normalization Rule describing the completed-read ("opened and closed") semantics and the last-write-wins sync. Revise `infrastructure.md`: add `onSetReadingBook` to the Realtime Sync handler list, with a one-line note on why it is a single last-write-wins event rather than paired open/close (same reasoning as the deliberate no-`onRatePrompt` decision). Do NOT touch `revealStartedAt` yet — that removal is Phase 2. [artifacts: datamodel, infrastructure]

## Phase 2: Self-guided Reveal View

- [x] T005 Rebuild `client/src/lib/views/Reveal.svelte` as a self-guided card grid: one card per book (cover art via the existing `generateCoverArt` as the card face), viewed/dirty dimming (a card the local viewer has opened renders less prominently). Clicking a card opens a per-book modal. In the modal: manual paging via click controls AND keyboard (arrows), with page-turn animations; page 1 shows the prompt in isolation, each later page shows the previous prompt-or-drawing above the newly revealed item; the last page offers the save-to-PNG button (reuse `exportBookToPng` from `client/src/lib/export/pngExport.ts`, mechanism unchanged); a "back to start" control resets that book to page 1; a "reveal all" control shows the full PNG strip. Page position and kept-place are per-viewer client-local state (closing preserves the page; reopening resumes). Emit the `setReadingBook` socket event on modal open (bookId) and close (null). Render "read by <player>" badges from `Room.bookReads` and "being read by <player>" badges from `Room.currentlyReading`. [F001] [F002] [artifacts: ui]
- [x] T006 Remove `Room.revealStartedAt` end-to-end now that its only consumer (Reveal.svelte's `computeRevealPosition` and the recompute interval / cadence constants / manual-override machinery) is gone with T005: delete the field from `shared/src/types.ts`, its `null` initializers in `roomStore.ts` (`createRoom`/`replayRoom`), and the `Date.now()` stamp on the `status = 'reveal'` transition in `onSubmitEntry` (`server/src/socket/handlers.ts`). Also drop the `revealStartedAt` row from the README datamodel mermaid diagram. Verify by grep + typecheck that no reference to `revealStartedAt` remains anywhere (shared/server/client/tests). Update any tests that referenced it. [F001] [artifacts: datamodel]
- [x] T007 Revise `ui.md`'s Reveal View section to describe the self-guided card-grid + per-book modal design (paging, page structure, kept-place, back-to-start, reveal-all, save-to-PNG on the last page, viewed/dirty dimming, and the read-by / being-read-by badges), replacing the timed auto-advance description. Drop the `prefers-reduced-motion` "gameplay-load-bearing pacing" carve-out (the pacing it guarded is gone). Remove the `revealStartedAt` field row and the "Reveal pacing (synchronized clock)" Normalization Rule from `datamodel.md`. Note the "read" ≈ "looked at" (opened-and-closed, not last-page-verified) wording. [F001] [artifacts: ui, datamodel]
- [x] T008 Add client tests for the reworked Reveal (in `client/src/lib/views/Reveal.test.ts`): read-by / being-read-by badges render from `bookReads` / `currentlyReading`; opening a modal emits `setReadingBook` with the bookId and closing emits it with `null`; keyboard + click paging advance the page and the previous-item-above-new-reveal pairing renders; kept-place resumes the prior page on reopen; the last page shows the save control. Confirm the full suite (including `pnpm run test:root`) stays green. [artifacts: ui]

## Phase 3: Host unread-books warning

- [ ] T009 On the reveal page's host "Play again" (`onPlayAgain`) and "End game" (`onEndGame`) controls, add a client-side confirm derived from `Room.bookReads` / `Room.currentlyReading` using the **per-book** trigger (approved): warn when some book has no completed read by any player (naming the unread books), or — when every book has a completed read but a modal is still open — name the still-open book(s). The confirm has a force-through path that emits the action anyway (the small-game-override pattern). No server change — the server stays force-through. Tests: the confirm fires when a book has no completed read and when a modal is open; force-through still emits `onPlayAgain` / `onEndGame`; no warning when every book is read and no modal is open. [F003] [artifacts: ui]
- [ ] T010 Revise `ui.md` to document the host unread-books warning on the reveal page — the per-book trigger, the two message variants (unread books; all-read-but-one-open), and the force-through behavior, cross-referencing the small-game override as the sibling force-anyway pattern. [F003] [artifacts: ui]
