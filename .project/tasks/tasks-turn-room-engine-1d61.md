---
plan: plan-turn-room-engine-2026-07-14-9249.md
generated: 2026-07-14
status: in-progress
---

# Tasks

## Phase 1: Round-gating

- [x] T001 [artifacts: datamodel] In `shared/src/turnAdvancement.ts`, write failing tests asserting: (a) when two books in the same room are at different `entries.length`, the book at the room-wide minimum returns its normal `NextEntry` from `computeNextEntry`, (b) a book ahead of that minimum (but not itself complete) returns `null` from `computeNextEntry` even though `position < room.players.length`. Confirm both fail against current behavior, then implement a `currentRoundFor(room: Room): number` helper (`Math.min(...room.books.map(b => b.entries.length))`, `0` for an empty `books` array) and add the round gate to `computeNextEntry`: return `null` when `position > currentRoundFor(room)`, in addition to the existing "book complete" check. Addresses feedback F001 in `.project/feedback/feedback-main-4af4.md`.
- [x] T002 [artifacts: datamodel] In `server/src/socket/handlers.test.ts`, write a failing test asserting `onSubmitEntry` returns `{ error: 'round-not-open' }` (distinct from `'book-complete'`) when the targeted book is ahead of the current room-wide round. Confirm failure, then update `onSubmitEntry` in `server/src/socket/handlers.ts` to check `book.entries.length >= room.players.length` first (→ `book-complete`), and otherwise, if `computeNextEntry` returned `null`, return `round-not-open`.
- [x] T003 [artifacts: datamodel] Add `roundStartedAt: number | null`, `timerExtensions: Record<string, number>`, `pendingTimeoutVote: TimeoutVote | null` to the `Room` interface and a `TimeoutVote` interface (`stalledPlayerIds: string[]`, `eligibleVoterIds: string[]`, `votes: Record<string, 'full' | 'half' | '15m' | 'force-empty'>`, `voteDeadline: number`) in `shared/src/types.ts`, and `emptyByTimeout?: boolean` to `Entry`. Write failing tests asserting: `onStartGame` sets `Room.roundStartedAt` to the current time when transitioning to `writing` and initializes `timerExtensions: {}`, `pendingTimeoutVote: null`; and `onSubmitEntry` resets `roundStartedAt` to the current time and clears `timerExtensions`/`pendingTimeoutVote` when a submission causes `currentRoundFor(room)` to increase (compare before/after the entry is pushed). Confirm failures, then implement in `server/src/domain/roomStore.ts` (`createRoom`/`createBooksForRoom` as needed) and `server/src/socket/handlers.ts`.
- [x] T004 [artifacts: ui] [parallel] In `client/src/lib/views/WritingDrawing.svelte`, write a failing test asserting a distinct "waiting for the round to finish" message is shown (not the prior generic wait state) when the current player has no entry offered by `computeNextEntries(room)` but does have an incomplete book of their own elsewhere in `room.books` — i.e. they finished their part of the current round and are waiting on others, as opposed to having nothing left to do. Confirm failure, then implement.

## Phase 2: Minimum player count

- [x] T005 [artifacts: datamodel] In `server/src/socket/handlers.test.ts`, write a failing test asserting `onStartGame` returns `{ error: 'too-few-players' }` when `room.players.length < 3` and the request's `acknowledgeSmallGame` is not `true`, and succeeds (transitions to `writing`) when `acknowledgeSmallGame: true` even with 1 player. Confirm failure, then add `acknowledgeSmallGame?: boolean` to `StartGameInput` (`server/src/socket/handlers.ts`) and the guard at the top of `onStartGame`, before the existing host-only check's transition logic.
- [ ] T006 [artifacts: ui] [parallel] In `client/src/lib/views/Lobby.test.ts`, write failing tests asserting: (a) player-count guidance text ("recommend 4+, minimum 3") is always visible to the host, (b) below 3 players an "I know this won't really work but I want to test something" checkbox appears and "start game" is disabled until checked, (c) at 3+ players the checkbox is absent and "start game" is enabled as today. Confirm failures, then implement in `Lobby.svelte`. Extend `SessionStore.startGame` (`client/src/lib/stores/session.ts`) to accept an `acknowledgeSmallGame` argument and pass it through to the `startGame` socket event.

## Phase 3: Turn timer control and countdown

- [ ] T007 [artifacts: datamodel] In `server/src/socket/handlers.test.ts`, write a failing test for a new `onSetTurnTimer` handler: accepted (sets `Room.turnTimerMinutes`) only when `playerId === room.hostPlayerId` and `room.status === 'lobby'`, for a value of `15 | 30 | 60 | 240 | 720 | null`; rejected with `not-host` or a status error otherwise. Confirm failure, then implement `onSetTurnTimer` in `server/src/socket/handlers.ts` and wire a `setTurnTimer` event to it in `server/src/socket/server.ts`.
- [ ] T008 [artifacts: ui] [parallel] In `client/src/lib/views/Lobby.test.ts`, write a failing test asserting the host-only timer selector (off/15m/30m/1hr/4hr/12hr) reflects `Room.turnTimerMinutes` and emits `setTurnTimer` with the chosen value on change. Confirm failure, then implement in `Lobby.svelte`.
- [ ] T009 [artifacts: ui] [parallel] In `WritingDrawing.svelte`, write a failing test asserting a countdown to `Room.roundStartedAt + (Room.timerExtensions[player.id] ?? Room.turnTimerMinutes * 60000)` is shown for the current player when `Room.turnTimerMinutes` is set, and no countdown is shown when it's `null`. Confirm failure, then implement.

## Phase 4: Timeout vote and background sweep

- [ ] T010 [artifacts: datamodel] In a new `server/src/domain/timerSweep.test.ts`, write failing tests for a pure `resolveTimeoutVote(room: Room, now: number): void` function: (a) a plurality of `full`/`half`/`15m` votes extends `Room.timerExtensions[playerId]` (by 15/30/60/240/720 minutes' worth of ms, or half that for `half`, or a flat 15 minutes for `15m`) for every player in `stalledPlayerIds` and clears `pendingTimeoutVote`; (b) a plurality (or zero-votes-cast) `force-empty` result inserts an `Entry` with `emptyByTimeout: true`, empty `content`, and the correct `position`/`type` for every stalled player on their respective book, then clears `pendingTimeoutVote`; (c) a tie among `full`/`half`/`15m`/`force-empty` resolves in that preference order (most lenient first). Confirm failures, then implement in `server/src/domain/timerSweep.ts`.
- [ ] T011 [artifacts: datamodel] In `timerSweep.test.ts`, write failing tests for a pure `sweepRoom(room: Room, now: number): void` function: (a) when every player in `computeNextEntries(room)`'s implied stalled set has passed their deadline and no vote is open, it opens `Room.pendingTimeoutVote` with the correct `stalledPlayerIds` and `eligibleVoterIds` (players who've submitted this round, or every player in the room if none have — including the stalled ones in that edge case) and a `voteDeadline` 2 minutes out; (b) when an open vote's `voteDeadline` has passed, it calls `resolveTimeoutVote`. Confirm failures, then implement.
- [ ] T012 In `server/src/socket/handlers.test.ts`, write a failing test for a new `onCastTimeoutVote` handler: records `{roomId, playerId, choice}` into `Room.pendingTimeoutVote.votes`, rejecting if no vote is pending or `playerId` isn't in `eligibleVoterIds`; once every eligible voter has voted, calls `resolveTimeoutVote` immediately rather than waiting for the sweep. Confirm failure, then implement in `handlers.ts` and wire a `castTimeoutVote` event in `server.ts`.
- [ ] T013 In `server/src/index.test.ts` (or a new bootstrap test file if none exists), write a failing test asserting the server entry point wires a 30-second `setInterval` that calls `sweepRoom` for every room in `store.rooms` with `status === 'writing'` and `turnTimerMinutes` set, broadcasting `roomUpdated` (via the Socket.IO server) to any room whose state changed as a result. Confirm failure, then implement — the interval callback itself belongs in `timerSweep.ts` (e.g. a `startTimerSweep(store, io)` export), with `server/src/index.ts` only calling it (constitution Principle X — entry point wires dependencies only).
- [ ] T014 [artifacts: ui] [parallel] In `WritingDrawing.svelte`, write a failing test asserting the timeout-vote prompt (naming the stalled player(s) by name and showing the four options) appears when `Room.pendingTimeoutVote` is present and the current player's id is in `eligibleVoterIds`, and that selecting an option emits `castTimeoutVote` with that choice. Confirm failure, then implement.
- [ ] T015 [parallel] Write failing tests asserting: `onCastTimeoutVote` and `sweepRoom`'s vote-open path each log a structured `timeout_vote_opened`/vote-recorded event; `resolveTimeoutVote`'s force-empty path logs via the existing `turn_advanced` event shape with `reason: 'timeout-forced-empty'` for each auto-submitted entry; and a `timeout_vote_resolved` event is logged with the winning choice. Confirm failures, then implement the logging calls (constitution Principle IX).
