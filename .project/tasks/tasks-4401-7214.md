---
plan: plan-4401-2026-07-14-7cf3.md
generated: 2026-07-14
status: ready
---

# Tasks

## Phase 1: onEndGame observability fix

- [ ] T001 [defect: 4056be9c] In `server/src/socket/handlers.test.ts`, write a failing test asserting `onEndGame` logs `{ event: 'game_completed', outcome: 'success', roomId, reason: 'host-ended' }` via a fake logger on the success path only — not when it rejects with `room-not-found` or `not-host`. Confirm the test fails against current `onEndGame` (which takes no `logger` parameter at all). Then add a `logger: Logger` parameter to `onEndGame` in `server/src/socket/handlers.ts` and the log call, placed right after `room.status = 'ended'` and before the `roomUpdated` broadcast. Update the `endGame` event registration in `server/src/socket/server.ts` to pass `logger` through, mirroring the call shape already used for `onSubmitEntry`/`onCreateRoom`/`onJoinRoom`. Run `pnpm run test`, `pnpm run typecheck`, and `pnpm run lint` before considering this task done.
