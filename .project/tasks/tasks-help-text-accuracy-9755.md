---
plan: plan-help-text-accuracy-2026-07-20-e6e1.md
generated: 2026-07-20
status: in-progress
---

# Tasks

## Phase 1: Handler-list correction

- [x] T001 [artifacts: infrastructure] [defect: c217e21f] Add `onSetPromptMode`, `onSetCuratedPromptCount`, and `onSetAllowPromptWriteIn` to the Realtime Sync handler list in `.project/artifacts/infrastructure.md`. All three are wired in `server/src/socket/server.ts` but absent from the artifact's list, which reads as exhaustive — so an agent consulting it to find the prompt-mode handlers concludes they do not exist. Verify against `server.ts` rather than trusting this description; if further handlers are also missing, add those too and say so. Documentation-only; no test requirement (constitution Principle III exempts documentation changes).

## Phase 2: Remove the false opening-phrase claim

- [x] T002 [artifacts: ui] Failing test first in `client/src/lib/components/RulesOverview.test.ts`, then fix the copy. `RulesOverview.svelte:30` says "A guest is handed a blank page and asked to write a phrase — anything they like." This is FALSE whenever `Room.promptMode === 'curated'`, where the player picks from a dealt hand (`WritingDrawing.svelte:240` renders a chooser, not an input; see `datamodel.md` Normalization Rules — Curated prompts). Rewrite so the sentence holds in BOTH modes. Keep the docent voice — match the surrounding register; this is a truth fix, not a tone change. (feedback F001)
- [x] T003 [artifacts: ui] Add a structural test asserting the rules panel makes no prompt-mode-specific claim — i.e. its copy contains no phrasing that presupposes free-form authorship. Assert on STRUCTURE, not exact wording (the plan's Complexity Tracking entry commits to this): a test pinned to a specific sentence breaks on every copy edit and will be deleted, which defeats the purpose. This is the regression guard for the class of bug T002 fixes.

## Phase 3: Explain the rating control

- [x] T004 Resolve the plan's Open Question 3 before writing any copy: decide whether the rating explanation lives inline near the control, in the rules panel, or both. Inline is discoverable exactly when the player is confused but adds turn-screen clutter; the rules panel is calmer but is not open at the moment of confusion. Record the decision and its reasoning in a comment at the implementation site. Decision task; no test requirement, but its output is required by T005.
- [x] T005 [artifacts: ui] Failing test first, then implement T004's decision. The thumbs control (`WritingDrawing.svelte:340-372`) currently ships with only the legend "Was this fun to draw?" and no explanation anywhere. The explanation must convey three things: the rating tunes the curated phrase deck, it is anonymous, and it is NEVER shown to anyone — including the player whose phrase it was. Without this a player sees thumbs on a phrase ANOTHER PLAYER WROTE and reasonably reads it as rating that person, which is the exact social dynamic `ui.md` designed out. Do NOT surface any rating value or tally in any view — `ui.md` forbids it and T006 asserts it. (feedback F002)
- [ ] T006 [artifacts: ui] Failing test first, then confirm the new copy did not accidentally introduce a rating readback. Assert no view renders a rating value, count, or tally — not the rater's own, not the phrase author's, not on Reveal. This overlaps the existing absence guard added with `player-prompt-rating`; extend rather than duplicate it if one already covers this. The risk being guarded is that "explain the rating" is a short step from "show the rating".

## Phase 4: Lobby tooltip coverage, and the artifact claim it settles

- [ ] T007 Resolve the plan's Open Question 2 before implementing: decide whether the existing curated-mode tooltip is extended to cover the prompt count selector and allow-write-in toggle, or whether each gets its own. One tooltip covering a cluster of related settings is a legitimate design. Treat the small-game acknowledgement checkbox (`Lobby.svelte:339`) as a SEPARATE call — it is the only one of the three uncovered controls that is not part of curated mode. Record the decision. Decision task; no test requirement, but its output is required by T008.
- [ ] T008 [artifacts: ui] Failing tests first, then implement T007's decision for the three uncovered Lobby host controls: the curated prompt count selector (`Lobby.svelte:307`), the allow-write-in toggle (`:325`), and the small-game acknowledgement (`:339`). Four `InfoTooltip` instances currently exist against seven host controls. Match the existing tooltip register — short, docent voice, explains the setting's CONSEQUENCE not just its label (see the four at 226/242/263/284 for the established pattern). (feedback F003)
- [ ] T009 [artifacts: ui] Add a structural test asserting every rendered host control in the Lobby has an associated info affordance. Derive the control set from the rendered output rather than a hardcoded list — a hardcoded list is the same failure mode as the artifact claim being fixed in T010, and would go stale the next time a setting is added. If T007 decided one tooltip legitimately covers several controls, encode THAT rule rather than a naive one-to-one mapping.
- [ ] T010 [artifacts: ui] [defect: f678931d] Reconcile `ui.md`'s claim that the `(?)` info-affordance pattern "is reused for every host-configurable setting in the Lobby View above" with what T008 actually implemented. The sentence is self-contradictory in its current form regardless — it asserts "every" and then enumerates four (force-monochrome, turn timer, laps-per-book, prompt-mode) while seven host controls exist. Either the claim is now true and the enumeration should be completed, or it must be narrowed to what is genuinely covered. MUST be done after T008, not before: the artifact's correctness depends on the code decision. Documentation-only; no test requirement.

## Phase 5: Rules panel completeness

- [ ] T011 Resolve the plan's Open Question 1 before extending anything: decide what the rules panel should cover. It currently explains only the base write/draw/reveal loop. A panel documenting every setting stops being an overview and duplicates the tooltips; a panel ignoring four shipped features misleads by omission. The plan's lean is to cover what changes the SHAPE of a game (laps, curated mode) and leave per-setting detail to tooltips — evaluate that rather than assuming it. Record the decision. Decision task; no test requirement, but its output is required by T012.
- [ ] T012 [artifacts: ui] Failing test first where a structural assertion is possible, then extend `RulesOverview.svelte` per T011's decision. It currently omits four shipped features: turn timers, laps per book, curated prompt mode, and prompt rating. Keep the docent voice and keep it an OVERVIEW — length is a real cost here, since a panel nobody finishes reading helps less than a short one. (feedback F004)
- [ ] T013 [artifacts: ui] Correct the rules panel's implication that a book passes around the circle exactly once. It currently reads "the book passes around the circle", while `Room.lapsPerBook` allows one to three full rotations (see `datamodel.md` Normalization Rules — Laps per book). Verify the actual range in the code before writing the copy rather than trusting this description. (feedback F004)
