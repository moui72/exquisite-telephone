---
plan: plan-curation-store-hardening-2026-07-20-d61f.md
generated: 2026-07-20
status: abandoned
---

# Tasks

## Phase 1: Artifact revisions

- [ ] T001 [artifacts: infrastructure] Rewrite `.project/artifacts/infrastructure.md`'s Curation Store section for append-only storage (feedback F002). Replace the **Shape** paragraph (currently "a single JSON file ... read once at startup into memory, mutated in place, and flushed back on a debounce") with one file per rating event, written once and never mutated, aggregated at read time. Replace the **Durability** paragraph (temp+fsync+rename, debounced flushes) — an append-only file needs no atomic rewrite and no debounce; crash safety is "at worst one partial trailing file". Resolve the standing "Revisit if the app ever runs more than one process, at which point concurrent writers make a file the wrong shape" caveat: append-only answers it now rather than deferring. Add the constraint that filenames are composed ONLY of server-controlled values (timestamp + random) with no player-influenced component. Documentation-only; no test requirement (constitution Principle III exempts documentation changes).
- [ ] T002 [artifacts: infrastructure] Remove two now-obsolete entries from `infrastructure.md`'s Production Annotations: the single-writer/volume-bound annotation (append-only files have no concurrent-writer correctness problem — but KEEP the volume-pins-to-one-machine fact, which the Deployment section states independently and which remains true) and the up-to-2s-lost-on-unclean-crash annotation (no debounce means nothing is buffered to lose). Documentation-only; no test requirement.
- [ ] T003 [artifacts: datamodel] Re-frame `.project/artifacts/datamodel.md`'s Persisted Entities section (feedback F002): the persisted *record* becomes a rating **event** (one per rating cast, carrying phrase, value, origin, and timestamp); `PromptRating` and `CandidatePhrase` remain as the **derived aggregate view** produced by folding the event log, not the on-disk shape. Keep the existing no-attribution property (no link to rater or author). Documentation-only; no test requirement.
- [ ] T004 [artifacts: datamodel] Add a normalization rule to `datamodel.md` for the `Entry.content` maximum length (feedback F001), stating that content is bounded at the submission boundary — ahead of both the room store and the curation store — so the cap protects in-memory game state as well as durable data, and that oversize submissions are rejected rather than silently truncated. Leave the concrete limits to T005/T006. Documentation-only; no test requirement.

## Phase 2: Bound entry content at the submission boundary

- [ ] T005 Resolve the plan's Open Question 1 before writing the cap: measure a realistic drawing's serialized payload size (replay an existing test fixture's stroke/fill ops through `JSON.stringify` and record the byte count) and a realistic text phrase's. Text and drawing payloads differ by orders of magnitude, so this is TWO limits, not one. Set each above the measured realistic maximum with clear margin rather than picking a round number. Record the measurements and the chosen limits in a comment beside the constants so a later reader knows they were derived, not invented. Research/measurement task; no test requirement, but its output is required by T006.
- [ ] T006 [artifacts: datamodel] Failing tests first, then enforce the T005 limits in the entry-submission path (`server/src/socket/handlers.ts`). Cover: an oversize TEXT entry is rejected; an oversize DRAWING payload is rejected; a realistic drawing near but under the limit is ACCEPTED (the regression guard that stops the cap being set too low and silently breaking real gameplay); rejection is a clear failure response, never a silent truncation. Enforce ahead of the room store, so oversize content never reaches in-memory game state either. (feedback F001)

## Phase 3: Append-only event store

- [ ] T007 Failing tests first, then add server-controlled filename generation to `server/src/domain/curationStore.ts`: names composed ONLY of a timestamp and a random suffix. Assert that NO part of any generated name derives from phrase text — including that two events with wildly different phrase content produce names differing only in timestamp/random. A sanitized slug of the phrase is explicitly the wrong shape; it puts attacker-influenced bytes in a path for no benefit the timestamp does not already provide. (feedback F002)
- [ ] T008 Failing test first, then add a path-containment guard: the write path resolves its target and refuses to write anywhere outside the configured curation directory. Test with traversal-shaped inputs (`../`, absolute paths, encoded variants) fed through whatever the naming function accepts. This is redundant against T007's generated names by design — it is what stops a FUTURE naming change from silently reintroducing traversal. See the plan's Complexity Tracking entry.
- [ ] T009 [artifacts: infrastructure] Failing tests first, then replace the store's write path with one file per rating event, written exactly once via an exclusive create (never read-modify-write, never rename-over). A write failure must be logged and swallowed, never thrown — a curation failure must never block a game turn (constitution Principle IX: log the failure, do not swallow it silently). (feedback F002)
- [ ] T010 Failing tests first, then implement the read path: fold the event files into the `PromptRating` / `CandidatePhrase` aggregate view. Cover: bank-phrase events accumulate into up/down tallies; player-written thumbs-up events upsert candidates by exact text, incrementing `votes` and preserving the earliest `firstLoggedAt`; a corrupt or partial trailing file is skipped with a logged warning rather than failing the whole fold (this is the crash case the append-only design accepts by construction).
- [ ] T011 Resolve the plan's Open Question 3, then implement accordingly: decide whether the fold runs at server boot or only on demand. Nothing in the running game reads the aggregate, so boot-time folding may be pure startup cost for zero benefit — the plan leans on-demand, which also leaves the backlogged `curation-data-aggregation-pipe` free to be the only reader. Record the decision and its reasoning in a comment. Test that whichever path is chosen does not run the other.
- [ ] T012 Resolve the plan's Open Question 2, then implement a bound on event accumulation (count, total bytes, or age). Whichever is chosen MUST fail safely — reaching the limit must never block or fail a game turn; it degrades curation only. Failing test first, including an explicit test that a turn still submits successfully with the store at its limit. Add a Production Annotation at the enforcement point describing what is discarded or refused at the limit. (feedback F001)

## Phase 4: Delete what the new shape made dead

- [ ] T013 Delete the debounce timer, `flush()`, and the temp+fsync+rename write path from `server/src/domain/curationStore.ts`, along with the tests that asserted those behaviors. Constitution Principle IV — the replaced approach is deleted in the same change, not archived in place or left "for reference". No new test; the deletion is verified by T015.
- [ ] T014 Delete the `gracefulShutdown` module (`server/src/domain/gracefulShutdown.ts` and its test) and its wiring in `server/src/index.ts`. It existed solely to drain the debounce before exit; with no buffer there is nothing to flush. Verify `index.ts` still starts cleanly and remains pure wiring (constitution Principle X).
- [ ] T015 Verify the full suite is green with the deleted code GONE rather than merely unreferenced: run `pnpm lint`, `pnpm typecheck`, `pnpm test`, and grep the codebase for lingering references to `flush`, `debounce`, and `gracefulShutdown` in the curation path. Confirm no test still asserts deleted behavior. Report the final test count.
