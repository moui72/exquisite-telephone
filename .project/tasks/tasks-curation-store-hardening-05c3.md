---
plan: plan-curation-store-hardening-2026-07-20-d61f.md
generated: 2026-07-20
status: in-progress
---

# Tasks

## Phase 1: Artifact revisions

- [x] T001 [artifacts: infrastructure] Rewrite `.project/artifacts/infrastructure.md`'s Curation Store section for append-only storage (feedback F002 of `feedback-curation-store-95c1.md`). Replace the **Shape** paragraph (currently "a single JSON file ... read once at startup into memory, mutated in place, and flushed back on a debounce") with one file per rating event, written once and never mutated, aggregated at read time. Replace the **Durability** paragraph (temp+fsync+rename, debounced flushes) — append-only needs no atomic rewrite and no debounce; crash safety is "at worst one partial trailing file". Resolve the standing "Revisit if the app ever runs more than one process, at which point concurrent writers make a file the wrong shape" caveat: append-only answers it now rather than deferring. Add the constraint that filenames are composed ONLY of server-controlled values (timestamp + random), no player-influenced component. Documentation-only; no test requirement (constitution Principle III exempts documentation changes).
- [x] T002 [artifacts: infrastructure] Remove two now-obsolete entries from `infrastructure.md`'s Production Annotations: the single-writer/volume-bound annotation (append-only files have no concurrent-writer correctness problem — but KEEP the volume-pins-to-one-machine fact, which remains true and is stated independently in Deployment) and the up-to-2s-lost-on-unclean-crash annotation (no debounce means nothing is buffered to lose). Documentation-only; no test requirement.
- [x] T003 [artifacts: infrastructure] Rewrite `infrastructure.md`'s **Deployment (Fly.io)** section for the real topology (feedback F001 of `feedback-deploy-channel-parity-add1.md`). It currently claims the app "deploys as a single Fly.io app running one process/container — matching Principle I (no premature scaling): one Dockerfile, one `fly.toml`", which is FALSE and is the root cause of repeated dual-channel amnesia: agents read artifacts before acting, so this actively misinforms them. Reality to record: two Fly apps (`exquisite-telephone`, `exquisite-telephone-beta`), two configs (`fly.toml`, `fly.staging.toml`), two API tokens (`FLY_API_TOKEN_PROD`/`FLY_API_TOKEN_BETA`), two CI deploy jobs with separate concurrency groups. Channel semantics: push to `main` deploys beta; prod deploys ONLY from the `release` branch, which only ever receives a FAST-FORWARD of `main` — which is why `.github/workflows/ci.yml` skips its `checks` job on `release`. Each channel has its own app, config, token, and volume. Note that Principle I is still satisfied — one machine per app — the "single app" phrasing was simply wrong. Documentation-only; no test requirement.
- [x] T004 [artifacts: infrastructure] In the Deployment section rewritten by T003, add an explicit inventory of the remaining ONE-TIME MANUAL CLI steps, labelled as manual, because these are precisely what gets forgotten: `fly volumes create <name> --app <app> --region <machine's actual region> --size 1` per app, and `fly scale count 1 -a <app>` per app. State that the volume region must match the RUNNING MACHINE's region (read via `fly status`), not necessarily `primary_region` in the config — a mismatch fails at machine start with no obviously storage-related error. Record that both volumes already exist and are verified: `vol_r681m3no1nq5ex14` (prod) and `vol_vp2l1gyjj3lw9me4` (beta), both 1GB in `iad`, matching their machines. Documentation-only; no test requirement.
- [x] T005 [artifacts: datamodel] Re-frame `.project/artifacts/datamodel.md`'s Persisted Entities section: the persisted *record* becomes a rating **event** (one per rating cast, carrying phrase, value, origin, and timestamp); `PromptRating` and `CandidatePhrase` remain as the **derived aggregate view** produced by folding the event log, not the on-disk shape. Keep the existing no-attribution property (no link to rater or author). Documentation-only; no test requirement.
- [x] T006 [artifacts: datamodel] Add a normalization rule to `datamodel.md` for the `Entry.content` maximum length (feedback F001 of `feedback-curation-store-95c1.md`), stating that content is bounded at the submission boundary — ahead of both the room store and the curation store — so the cap protects in-memory game state as well as durable data, and that oversize submissions are rejected rather than silently truncated. Leave concrete limits to T009/T010. Documentation-only; no test requirement.

## Phase 2: Beta/prod config parity

- [x] T007 [artifacts: infrastructure] Add the `[mounts]` block (`source = "curation_data"`, `destination = "/data"`) and the `CURATION_DATA_PATH = "/data/curation.json"` env var to `fly.staging.toml`, matching `fly.toml` (feedback F002 of `feedback-deploy-channel-parity-add1.md`). Without these, beta auto-deploys from `main` on every push and the Curation Store falls back to its local dev default path INSIDE the container, silently discarding every rating beta collects — no error, no missing volume, just no data. Beta's volume `vol_vp2l1gyjj3lw9me4` already exists in `iad`, so there is no window where a declared mount has no volume behind it. Config change; no test requirement, but verify by diffing the two TOMLs and confirming they now differ ONLY in `app` (and any comments).
- [x] T008 Correct `fly.staging.toml`'s header comment, which currently claims "Mirrors fly.toml; only `app` differs" — false until T007, and never enforcement in the first place. Replace it with an accurate statement plus a pointer to the backlogged `fly-config-lockstep` feature as the real fix (lint or generation). Do NOT build the lockstep mechanism here; it is explicitly out of this plan's scope. Documentation-only; no test requirement.

## Phase 3: Bound entry content at the submission boundary

- [x] T009 Resolve the plan's Open Question 1 before writing the cap: measure a realistic drawing's serialized payload size (replay an existing test fixture's stroke/fill ops through `JSON.stringify` and record the byte count) and a realistic text phrase's. Text and drawing payloads differ by orders of magnitude, so this is TWO limits, not one. Set each above the measured realistic maximum with clear margin rather than picking a round number. Record the measurements and chosen limits in a comment beside the constants so a later reader knows they were derived, not invented. Research/measurement task; no test requirement, but its output is required by T010.
- [x] T010 [artifacts: datamodel] Failing tests first, then enforce the T009 limits in the entry-submission path (`server/src/socket/handlers.ts`). Cover: an oversize TEXT entry is rejected; an oversize DRAWING payload is rejected; a realistic drawing near but under the limit is ACCEPTED — this last case is the regression guard that stops the cap being set too low and silently breaking real gameplay while the rejection tests still pass. Rejection is a clear failure response, never silent truncation. Enforce ahead of the room store so oversize content never reaches in-memory game state either.

## Phase 4: Append-only event store

- [x] T011 Failing tests first, then add server-controlled filename generation to `server/src/domain/curationStore.ts`: names composed ONLY of a timestamp and a random suffix. Assert that NO part of any generated name derives from phrase text — including that two events with wildly different phrase content produce names differing only in timestamp/random. A sanitized slug of the phrase is explicitly the wrong shape: it puts attacker-influenced bytes in a path for no benefit the timestamp does not already provide.
- [x] T012 Failing test first, then add a path-containment guard: the write path resolves its target and refuses to write anywhere outside the configured curation directory. Test with traversal-shaped inputs (`../`, absolute paths, encoded variants) fed through whatever the naming function accepts. This is redundant against T011's generated names BY DESIGN — it is what stops a FUTURE naming change from silently reintroducing traversal. See the plan's Complexity Tracking entry.
- [x] T013 [artifacts: infrastructure] Failing tests first, then replace the store's write path with one file per rating event, written exactly once via an exclusive create (never read-modify-write, never rename-over). A write failure must be logged and swallowed, never thrown — a curation failure must never block a game turn (constitution Principle IX: log the failure, do not swallow it silently).
- [x] T014 Failing tests first, then implement the read path: fold the event files into the `PromptRating`/`CandidatePhrase` aggregate view. Cover: bank-phrase events accumulate into up/down tallies; player-written thumbs-up events upsert candidates by exact text, incrementing `votes` and preserving the earliest `firstLoggedAt`; a corrupt or partial trailing file is skipped with a logged warning rather than failing the whole fold — this is the crash case the append-only design accepts by construction.
- [x] T015 Resolve the plan's Open Question 3, then implement accordingly: decide whether the fold runs at server boot or only on demand. Nothing in the running game reads the aggregate, so boot-time folding may be pure startup cost for zero benefit — the plan leans on-demand, which also leaves the backlogged `curation-data-aggregation-pipe` free to be the only reader. Record the decision and its reasoning in a comment. Test that whichever path is chosen does not run the other.
- [x] T016 Resolve the plan's Open Question 2, then implement a bound on event accumulation (count, total bytes, or age). Whichever is chosen MUST fail safely — reaching the limit must never block or fail a game turn; it degrades curation only. Failing test first, including an explicit test that a turn still submits successfully with the store at its limit. Add a Production Annotation at the enforcement point describing what is discarded or refused at the limit.

## Phase 5: Delete what the new shape made dead

- [x] T017 Delete the debounce timer, `flush()`, and the temp+fsync+rename write path from `server/src/domain/curationStore.ts`, along with the tests that asserted those behaviors. Constitution Principle IV — the replaced approach is deleted in the same change, not archived in place or left "for reference". No new test; the deletion is verified by T019.
- [x] T018 Delete the `gracefulShutdown` module (`server/src/domain/gracefulShutdown.ts` and its test) and its wiring in `server/src/index.ts`. It existed solely to drain the debounce before exit; with no buffer there is nothing to flush. Verify `index.ts` still starts cleanly and remains pure wiring (constitution Principle X).
- [ ] T019 Verify the full suite is green with the deleted code GONE rather than merely unreferenced: run `pnpm lint`, `pnpm typecheck`, `pnpm test`, and grep the codebase for lingering references to `flush`, `debounce`, and `gracefulShutdown` in the curation path. Confirm no test still asserts deleted behavior. Report the final test count.
