---
plan: plan-reveal-page-2026-07-14-b586.md
generated: 2026-07-14
status: in-progress
---

# Tasks

## Phase 1: Data model + server

- [x] T001 [artifacts: datamodel] Add `playAgainVotes: string[]` to the `Room` interface in `shared/src/types.ts`. Update every existing `Room` literal fixture across `shared/`, `server/`, and `client/` test files to include it (`playAgainVotes: []`), mirroring the pattern from the last two rounds of `Room` field additions (`monochromeOnly`, then `turnTimerMinutes`/`roundStartedAt`/`timerExtensions`/`pendingTimeoutVote`). Update `shared/src/types.test.ts`'s `Room` assertion to include the new field. Run `pnpm run typecheck` to catch any missed fixture.
- [x] T002 [artifacts: datamodel] In `server/src/socket/handlers.test.ts`, write a failing test asserting a new `onVoteToPlayAgain` handler adds `playerId` to `Room.playAgainVotes` and broadcasts `roomUpdated`; and a second test asserting voting twice with the same `playerId` doesn't create a duplicate entry. Confirm failures, then implement `onVoteToPlayAgain` in `server/src/socket/handlers.ts` and wire a `voteToPlayAgain` event in `server/src/socket/server.ts`.
- [x] T003 [artifacts: datamodel] In `server/src/domain/roomStore.test.ts`, write failing tests for a new `replayRoom(store: RoomStore, oldRoom: Room): { room: Room; playerIdMap: Map<string, Player> }`: (a) the returned room has a fresh `id` (different from `oldRoom.id`), `status: 'lobby'`, `playAgainVotes: []`, and the other `createRoom`-style defaults (`monochromeOnly: false`, `turnTimerMinutes: null`, etc.); (b) it creates exactly one new `Player` per player in `oldRoom.players`, each with a new `id` and new `sessionToken`, the same `name`, and `connected` carried over from the old player; (c) `playerIdMap` maps each old player's id to their corresponding new `Player`; (d) the new room's `hostPlayerId` equals the new player mapped from `oldRoom.hostPlayerId`. Confirm failures, then implement `replayRoom` in `server/src/domain/roomStore.ts`.
- [ ] T004 [artifacts: datamodel, infrastructure] In `server/src/socket/server.test.ts` (the existing real-Socket.IO-server integration style, matching how `onSetMonochrome`/`onEndGame` are tested there), write a failing test: two clients join a room, the room is put into `status: 'reveal'`, the host emits `playAgain`; assert (a) the host's ack contains a new `room`/`player` distinct from the old ones, (b) the *other* connected client receives a `roomChanged` event containing its own distinct new `player.id` (not the host's), with the same new `room.id` as the host's ack, (c) a fake logger recorded `{ event: 'room_created', outcome: 'success', reason: 'play-again', previousRoomId: <old room id> }`. Confirm failure, then implement `onPlayAgain` in `server/src/socket/handlers.ts`: host-only, only while `room.status === 'reveal'`, using `replayRoom` plus a local-socket loop — get the old room's connected socket IDs via `io.sockets.adapter.rooms.get(oldRoomId)`, resolve each via `io.sockets.sockets.get(id)` (real local `Socket` objects; this is a single-process app, no need for Socket.IO's cross-process `fetchSockets()` adapter API), and for each: look up its `socket.data.playerId` in `playerIdMap`, `socket.leave(oldRoomId)` / `socket.join(newRoom.id)`, update `socket.data.playerId`/`roomId`, issue a fresh session token via `sessionStore.issue(newPlayer.id, newRoom.id)` and set it on the new `Player`, then `socket.emit('roomChanged', { room: newRoom, player: newPlayer })`. Update `createSocketServer` in `server/src/socket/server.ts` to wire a `playAgain` event, passing `io` through to `onPlayAgain` (a new parameter no other handler currently needs).

## Phase 2: Client end-of-game controls

- [ ] T005 [artifacts: ui] In `client/src/lib/stores/session.test.ts`, write failing tests for four new `SessionStore` methods: `endGame()` emits `endGame` with `{roomId, playerId}` (mirroring `setMonochrome`'s shape); `leaveGame()` clears the stored session token (`SESSION_TOKEN_STORAGE_KEY`) and resets state to `{room: null, player: null, error: null, reconnecting: false}` **without** emitting any socket event; `voteToPlayAgain()` emits `voteToPlayAgain` with `{roomId, playerId}`; `playAgain()` emits `playAgain` with `{roomId, playerId}` and applies the ack the same way `createRoom` does (updates `room`/`player`, stores the new token). Also write a test asserting a new `roomChanged` socket listener updates **both** `room` and `player` from its payload (unlike the existing `roomUpdated` listener, which only updates `room`) and stores the new session token. Confirm failures, then implement all of it in `client/src/lib/stores/session.ts`.
- [ ] T006 [artifacts: ui] In `client/src/App.test.ts`, write a failing test asserting `App.svelte` renders an `ended` branch — text matching "This game has ended" and a "Return to home" button that calls `session.leaveGame()` on click — when `state.room.status === 'ended'`, distinct from the existing `state.error === 'game-ended'` rejoin-failure branch (verify both remain independently reachable). Confirm failure, then add the branch to `client/src/App.svelte`.
- [ ] T007 [artifacts: ui] Convert `client/src/lib/views/Reveal.svelte` from its current `export let room: Room` prop to `export let session: SessionStore = defaultSession` (matching `Lobby.svelte`/`WritingDrawing.svelte`'s pattern exactly — `$: state = $session`, reading `state.room`/`state.player` internally instead of a passed-in `room`). Update `client/src/App.svelte` to pass `session={session}` instead of `room={state.room}` to `<Reveal>`. Update every existing test case in `client/src/lib/views/Reveal.test.ts` to render with a fake `session` store (mirroring `Lobby.test.ts`'s `makeFakeSession` helper) instead of a bare `room` prop — this is a mechanical prop-plumbing change; confirm every existing assertion still passes unchanged after the fixture update (no new test needed for this task itself).
- [ ] T008 [artifacts: ui] [parallel] In `Reveal.test.ts`, write failing tests asserting: a non-host player sees "Leave game" and "Vote to play again" buttons, and clicking each calls `session.leaveGame()` / `session.voteToPlayAgain()` respectively; the host sees "End game" and "Play again" instead (not the non-host pair), clicking each calls `session.endGame()` / `session.playAgain()`; the host additionally sees a readiness count reflecting `Room.playAgainVotes.length` out of `Room.players.length` (e.g. "1 of 2 ready") that a non-host does not see. Confirm failures, then implement the four buttons and the readiness count in `Reveal.svelte`, added alongside (not replacing) the existing static book-grid content.

## Phase 3: Animated reveal viewer

- [ ] T009 [artifacts: ui] In a new `client/src/lib/reveal/coverArt.test.ts`, write failing tests for `generateCoverArt(seed: string)`: calling it twice with the same seed produces identical output (deterministic); two different seeds produce different output (not a hardcoded constant). Confirm failures, then implement in a new `client/src/lib/reveal/coverArt.ts` — hash the seed string into a small set of HSL colors and positions for a handful of overlapping shapes, pure function, no external dependency.
- [ ] T010 [artifacts: ui] In `Reveal.test.ts`, write failing tests (using Vitest fake timers) asserting: on initial render, only the first book's cover is shown (origin author's name + `generateCoverArt` output, seeded by `originAuthorId`) with no entries visible yet; after advancing fake time past the 2.5s initial delay plus one 4s tick, up to 2 entries become visible; after enough further ticks, the book's full chain is shown and the view has moved on to the next book's cover; a "show everything" control immediately renders every book's full chain (all entries, all books) regardless of remaining timer state; manual "previous"/"next" controls step between books without needing to wait for the timer. Confirm failures, then implement in `Reveal.svelte`: `currentBookIndex` and a per-book revealed-entry count as component state, a `setInterval` registered in `onMount` and cleared in `onDestroy` (matching `WritingDrawing.svelte`'s existing countdown-timer cleanup pattern) that, after the initial 2.5s cover delay, ticks every 4s revealing up to 2 more entries or advancing to the next book, and the three manual controls (mutating the same state directly, and resetting the interval's timing so a manual action isn't immediately overridden by the next tick). Reuse the existing full-book-grid rendering (from before this conversion) as the "show everything" end state rather than rewriting it.
- [ ] T011 [artifacts: ui] [parallel] Update the pre-existing "renders each book's full ordered chain of entries" test in `Reveal.test.ts` (written before the animated default existed) so it drives the "show everything" control first, then asserts the full-grid rendering — keeping it a meaningful check that the reused full-grid code path still works, without duplicating the animated-state coverage already added in T010.
