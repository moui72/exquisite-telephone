---
status: approved
branch: main
created: 2026-07-19
features: []
surfaced-defects: [62ab502f]
---

# Plan: document the missing onSetLapsPerBook handler

## Goal

Bring `infrastructure.md`'s Realtime Sync handler list back in sync with
the code by adding the undocumented `onSetLapsPerBook` handler.

## Scope

In scope:
- Add `onSetLapsPerBook` to `infrastructure.md`'s Realtime Sync handler
  list (defect `62ab502f`).

Out of scope:
- Any other artifact change. This is a pure documentation fix — the
  handler itself already exists and works correctly
  (`server/src/socket/handlers.ts:221`, registered in
  `server/src/socket/server.ts:16`); nothing in code changes.
- The two `ui.md` documented-but-undocumented-scope gaps flagged
  alongside this defect in the last `/ardd-defects` run (the `font-title`
  Uncial Antiqua treatment and the Foyer `GiltFrame` wrap) — those
  weren't surfaced as defects (this skill only surfaces `DEFECTS.md`
  entries) and belong to a separate `/ardd-refine ui` pass.

## Technical Approach

`infrastructure.md`'s Realtime Sync section lists the complete set of
Socket.IO handlers as prose. `onSetLapsPerBook` was added when
`configurable-book-laps-per-gam` shipped but never added to this list.
The fix is a one-line addition to that list — no design decision
involved, since the handler's actual behavior (setting
`Room.lapsPerBook`, host-only, lobby-only) is already fully documented
elsewhere in `datamodel.md`'s Normalization Rules and `ui.md`'s Lobby
View.

## Phase Breakdown

### Phase 1: Documentation fix
- T001 [artifacts: infrastructure] [defect: 62ab502f] Add
  `onSetLapsPerBook` to `infrastructure.md`'s Realtime Sync handler
  list, in the same style as the other listed handlers. Stamp
  `last_updated` to today.

## Open Questions

- None.

## Production Annotation Summary

- None — no production shortcut involved.
